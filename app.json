{
  "name": "zoo",
  "addons": [
    "heroku-postgresql"
  ],
  "scripts": {
    "test": "bin/rails test && bin/rails test:system"
  },
  "environments": {
    "test": {
      "buildpacks": [
        { "url": "https://github.com/heroku/heroku-buildpack-xvfb-google-chrome" },
        { "url": "https://github.com/appleton/heroku-buildpack-chromedriver#shim-chrome-location" },
        { "url": "heroku/ruby" }
      ]
    }
  }
}
